# prettier-cofig

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build prettier-cofig` to build the library.

## Running unit tests

Run `nx test prettier-cofig` to execute the unit tests via [Vitest](https://vitest.dev/).
